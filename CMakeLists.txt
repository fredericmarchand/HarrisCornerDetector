cmake_minimum_required(VERSION 2.8)
project( cornerDetector )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( cornerDetector harris_corner_template.cpp )
target_link_libraries( cornerDetector ${OpenCV_LIBS} )
